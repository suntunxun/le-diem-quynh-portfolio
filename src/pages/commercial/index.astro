---
export const prerender = true;
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import { SITE_TITLE } from '../../consts';
import { getCollection } from 'astro:content';
import { slugify } from '../../utils/slugify';

const works = (await getCollection('works'))
	.filter((w: any) => w.data.category === 'commercial')
	.sort((a: any, b: any) => {
		if (a.data.order !== b.data.order) {
			return a.data.order - b.data.order;
		}
		return b.data.year - a.data.year;
	});
---

<!doctype html>
<html lang="vi">
	<head>
		<BaseHead title={`Commercial — ${SITE_TITLE}`} description="Commercial work directed by Le Diem Quynh." />
	</head>
	<body>
		<Header />
		<main>
			<div class="page-header">
				<h1>COMMERCIAL</h1>
				<hr />
			</div>
			<div class="grid">
				{works.map((work: any) => (
					<a href={`/commercial/${slugify(work.data.title)}`} class="card">
						<div class="thumb">
							{work.data.thumbnail
								? <img src={work.data.thumbnail} alt={work.data.title} loading="lazy" />
								: <div class="thumb-empty" />
							}
						</div>
						<p class="card-title">{work.data.title}</p>
					</a>
				))}
				{works.length === 0 && (
					<p class="empty">No commercial work yet. Content will be added via CMS.</p>
				)}
			</div>
		</main>
	</body>
</html>

<style>
	main {
		/* Inherit from global.css */
	}
	.page-header h1 {
		font-size: 2.2rem;
		font-weight: 700;
		letter-spacing: 0.15em;
		margin: 2rem 0 1.2rem;
		color: #fff;
		text-align: center;
	}
	hr {
		border: none;
		border-top: 1px solid rgba(255, 255, 255, 0.2);
		margin: 0 0 2.5rem;
	}
	.grid {
		display: grid;
		grid-template-columns: repeat(4, 1fr);
		gap: 2.5rem 2rem;
	}
	.card {
		text-decoration: none;
		color: #fff;
		display: block;
	}
	.thumb {
		aspect-ratio: 16 / 9;
		overflow: hidden;
		background: #111;
	}
	.thumb img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		transition: opacity 0.2s;
		display: block;
	}
	.card:hover .thumb img {
		opacity: 0.8;
	}
	.thumb-empty {
		width: 100%;
		height: 100%;
		background: #1a1a1a;
	}
	.card-title {
		margin: 1rem 0 0;
		font-size: 1.1rem;
		font-weight: 500;
		color: rgba(255, 255, 255, 0.9);
		line-height: 1.4;
		text-align: center;
	}
	.empty {
		color: rgba(255, 255, 255, 0.4);
		font-style: italic;
	}
	@media (max-width: 1200px) {
		.grid {
			grid-template-columns: repeat(3, 1fr);
		}
	}
	@media (max-width: 768px) {
		.grid {
			grid-template-columns: repeat(2, 1fr);
			gap: 1.5rem 1rem;
		}
		.page-header h1 {
			font-size: 1.8rem;
		}
	}
</style>
